# Development (SQLite)
DATABASE_URL=sqlite+aiosqlite:///./scs_designer.db

# Production (PostgreSQL) — use in docker-compose
# DATABASE_URL=postgresql+asyncpg://scs:scs@db:5432/scs_designer

POSTGRES_USER=scs
POSTGRES_PASSWORD=scs
POSTGRES_DB=scs_designer

API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
